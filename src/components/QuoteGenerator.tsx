import React from 'react'

function QuoteGenerator() {
  // const [jokeData, setJokeData]: any = useState()


  // async function jokes() {
  //   const fetchJokesData = await fetch('https://icanhazdadjoke.com/j/<joke_id>')
  //   const data = await fetchJokesData.json()
  //   setJokeData(data)
  // }
    


  // useEffect(() => {
  //   jokes()
  // }, [])

  // useEffect(() => {
  //   if (jokeData) {
  //     console.log(jokeData.id)
  //   }

  // }, [jokeData])

  return (
    <div>
     
      {/* {animalData && animalData.species[0].description}
      
      {animalData
        && <img src={`url({animalData.species[0].image.url})`} alt="image" />
      } */}
    </div>
  )
}

export default QuoteGenerator
